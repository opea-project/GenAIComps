# Copyright (C) 2024 Prediction Guard, Inc
# SPDX-License-Identifier: Apache-2.0

version: "3.8"

services:
  embedding:
    image: opea/toxicity_predictionguard:latest
    container_name: toxicity-predictionguard
    ports:
      - "9090:9090"
    ipc: host
    environment:
      no_proxy: ${no_proxy}
      http_proxy: ${http_proxy}
      https_proxy: ${https_proxy}
      PREDICTIONGUARD_API_KEY: ${PREDICTIONGUARD_API_KEY}
    restart: unless-stopped

networks:
  default:
    driver: bridge