# Copyright (C) 2024 Intel Corporation
# SPDX-License-Identifier: Apache-2.0

# this file should be run in the root of the repo
services:
  lvm:
    build:
      dockerfile: comps/lvms/transformers/Dockerfile
    image: ${REGISTRY:-opea}/lvm:${TAG:-latest}
  # Xeon CPU
  llava:
    build:
      dockerfile: comps/lvms/transformers/llava/Dockerfile
    image: ${REGISTRY:-opea}/llava:${TAG:-latest}
  # Gaudi2 HPU
  llava-hpu:
    build:
      dockerfile: comps/lvms/transformers/llava/Dockerfile_hpu
    image: ${REGISTRY:-opea}/llava-hpu:${TAG:-latest}
  lvm-tgi:
    build:
      dockerfile: comps/lvms/Dockerfile_tgi
    image: ${REGISTRY:-opea}/lvm-tgi:${TAG:-latest}
  llava-tgi:
    build:
      context: tgi-gaudi
      dockerfile: Dockerfile
    image: ${REGISTRY:-opea}/llava-tgi:${TAG:-latest}
  lvm-predictionguard:
    build:
      dockerfile: comps/lvms/predictionguard/Dockerfile
    image: ${REGISTRY:-opea}/lvm-predictionguard:${TAG:-latest}
